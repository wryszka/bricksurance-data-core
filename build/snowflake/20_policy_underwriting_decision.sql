-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy.underwriting_decision (
  underwriting_decision_id VARCHAR NOT NULL COMMENT 'Identifier for the decision.',
  quote_id VARCHAR NOT NULL COMMENT 'The quote decided on.',
  underwriting_decision_type_code VARCHAR NOT NULL COMMENT 'The outcome.',
  appetite_rule_id VARCHAR COMMENT 'The appetite rule the decision applied, where rule-driven.',
  decided_by VARCHAR NOT NULL COMMENT 'The human underwriter or named agent/system that decided.',
  decided_by_agent BOOLEAN NOT NULL COMMENT 'True when the decision was taken autonomously by software.',
  decided_at TIMESTAMP_NTZ NOT NULL COMMENT 'When the decision was taken.',
  rationale VARCHAR COMMENT 'Free-text rationale, human- or agent-written.'
)
COMMENT = 'A recorded underwriting decision on a quote: the outcome, who or what decided (human underwriter or named agent), and the appetite rule applied. Machine decisions are audited to exactly the same standard as human ones - that is the condition for letting agents underwrite at all. Grain: One row per decision per quote.';

ALTER TABLE BRICKSURANCE.policy.underwriting_decision ADD CONSTRAINT pk_underwriting_decision PRIMARY KEY (underwriting_decision_id);
