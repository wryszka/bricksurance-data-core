-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.statement_line (
  statement_line_id VARCHAR NOT NULL COMMENT 'Identifier for the mapping row.',
  statement_type_code VARCHAR NOT NULL COMMENT 'Which primary statement the line belongs to.',
  reporting_regime_code VARCHAR NOT NULL COMMENT 'Presentation regime this mapping is for.',
  line_label VARCHAR NOT NULL COMMENT 'The statement line item, e.g. ''Net earned premium'' or ''Total assets''.',
  line_order NUMBER(38,0) NOT NULL COMMENT 'Presentation order within the statement.',
  account_id VARCHAR NOT NULL COMMENT 'The ledger account that rolls up into this line.',
  sign NUMBER(38,0) NOT NULL COMMENT '+1 or -1 applied to the account balance for presentation.'
)
COMMENT = 'The mapping from ledger accounts to financial-statement line items - how the chart of accounts becomes a balance sheet, income statement and cash flow. Different regimes present differently, so a line carries its regime; this is what lets the same ledger produce a Solvency II balance sheet and an IFRS/statutory one. Grain: One row per statement line item per account per regime.';

ALTER TABLE BRICKSURANCE.finance.statement_line ADD CONSTRAINT pk_statement_line PRIMARY KEY (statement_line_id);
