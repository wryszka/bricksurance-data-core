-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.pricing.rating_factor (
  rating_factor_id VARCHAR NOT NULL COMMENT 'Identifier for the rating factor.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the factor applies to.',
  name VARCHAR NOT NULL COMMENT 'Factor name as used in rating, e.g. vehicle_group.',
  description VARCHAR COMMENT 'What the factor represents, in business language.',
  rating_factor_kind_code VARCHAR NOT NULL COMMENT 'How the factor acts on premium.',
  classification VARCHAR NOT NULL COMMENT 'Data classification of the underlying input - internal, confidential or pii.'
)
COMMENT = 'A factor used to rate a line of business - the named driver premium responds to (e.g. vehicle group, flood band, mileage). Factors are data, so a rating build-up is inspectable rather than buried in engine code. Grain: One row per rating factor per line of business.';

ALTER TABLE BRICKSURANCE.pricing.rating_factor ADD CONSTRAINT pk_rating_factor PRIMARY KEY (rating_factor_id);
