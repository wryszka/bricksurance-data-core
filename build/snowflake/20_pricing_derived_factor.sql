-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.pricing.derived_factor (
  derived_factor_id VARCHAR NOT NULL COMMENT 'Identifier for the applied-factor row.',
  subject_kind_code VARCHAR NOT NULL COMMENT 'Whether the factor was applied to a quote or a policy.',
  subject_id VARCHAR NOT NULL COMMENT 'Identifier of the quote or policy the factor was applied to.',
  rating_factor_id VARCHAR NOT NULL COMMENT 'The rating factor applied.',
  level_code VARCHAR NOT NULL COMMENT 'The factor level selected for the risk.',
  relativity DECIMAL(10,4) COMMENT 'Relativity applied, copied from the level in force at rating time.',
  contribution DECIMAL(18,2) COMMENT 'Premium contribution of this factor, in the quote currency.'
)
COMMENT = 'The factor level and contribution applied to a specific quote or policy - the rating build-up made concrete. Summing the contributions reconstructs the quoted premium, so a price is always explainable to the factors that made it. Grain: One row per rating factor applied to a quote or policy.';

ALTER TABLE BRICKSURANCE.pricing.derived_factor ADD CONSTRAINT pk_derived_factor PRIMARY KEY (derived_factor_id);
