-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.renewal.retention_response_curve (
  retention_response_curve_id VARCHAR NOT NULL COMMENT 'Identifier for the curve point.',
  segment VARCHAR NOT NULL COMMENT 'Customer or product segment the curve applies to.',
  price_walk_band VARCHAR NOT NULL COMMENT 'Price-walk band, e.g. 0-5%, 5-10%, 10-20%, 20%+.',
  renewal_probability DECIMAL(6,4) NOT NULL COMMENT 'Modelled probability of renewal in this band, 0 to 1.',
  observations NUMBER(38,0) COMMENT 'Number of observed cases the point was calibrated on.'
)
COMMENT = 'Observed renewal probability by segment and price-walk band - the demand curve the renewal loop draws outcomes from. Calibrated from the same logit machinery the gen2 pricing repo built for new-business elasticity (reused, not re-derived). Higher price walk means lower retention, monotonically, within a segment. Grain: One row per segment per price-walk band.';

ALTER TABLE BRICKSURANCE.renewal.retention_response_curve ADD CONSTRAINT pk_retention_response_curve PRIMARY KEY (retention_response_curve_id);
