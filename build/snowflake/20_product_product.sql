-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.product (
  product_id VARCHAR NOT NULL COMMENT 'Identifier for the product.',
  product_code VARCHAR NOT NULL COMMENT 'Marketing/product code, unique within the source system.',
  name VARCHAR NOT NULL COMMENT 'Product name as marketed.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the product is written under.',
  product_status_code VARCHAR NOT NULL COMMENT 'Lifecycle status.',
  base_rate DECIMAL(10,6) COMMENT 'Indicative base rate per unit of sum insured, used for indicative quotes only.',
  currency_code VARCHAR NOT NULL COMMENT 'Default currency of the product.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A machine-readable insurance product: what is offered, under which line of business, at which base rate. Products are what quotes are requested against - by humans, aggregators or buyer agents alike - and what wordings, questions and appetite rules attach to. Grain: One row per product version offered.';

ALTER TABLE BRICKSURANCE.product.product ADD CONSTRAINT pk_product PRIMARY KEY (product_id);
