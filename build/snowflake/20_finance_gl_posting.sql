-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.gl_posting (
  gl_posting_id VARCHAR NOT NULL COMMENT 'Identifier for the posting.',
  journal_reference VARCHAR NOT NULL COMMENT 'Journal the posting belongs to.',
  gl_account_code VARCHAR NOT NULL COMMENT 'Ledger account posted to.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the stated currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  posting_date DATE NOT NULL COMMENT 'Ledger posting date.',
  policy_id VARCHAR COMMENT 'Source policy, where the posting derives from policy money.',
  claim_id VARCHAR COMMENT 'Source claim, where the posting derives from claim money.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A ledger posting derived from an operational transaction - the thin, auditable bridge to finance. Every posting cites its source policy or claim, so ledger totals reconcile to operational sums by construction. Grain: One row per posting per source transaction.';

ALTER TABLE BRICKSURANCE.finance.gl_posting ADD CONSTRAINT pk_gl_posting PRIMARY KEY (gl_posting_id);
