-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.tax_transaction (
  tax_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the movement.',
  legal_entity_id VARCHAR NOT NULL COMMENT 'The entity the tax relates to.',
  tax_type_code VARCHAR NOT NULL COMMENT 'Kind of tax.',
  policy_id VARCHAR COMMENT 'Source policy, for premium tax.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the stated currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Booking date.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A tax movement for a legal entity - insurance premium tax, corporation tax, deferred tax. IPT in particular is a real line on every premium; this makes the tax position queryable rather than buried. Grain: One row per tax movement per entity.';

ALTER TABLE BRICKSURANCE.finance.tax_transaction ADD CONSTRAINT pk_tax_transaction PRIMARY KEY (tax_transaction_id);
