-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.enrichment.motor_telematics_aggregate (
  telematics_aggregate_id VARCHAR NOT NULL COMMENT 'Identifier for the telematics aggregate record.',
  policy_id VARCHAR NOT NULL COMMENT 'Motor policy the behaviour was observed for.',
  period_start_date DATE NOT NULL COMMENT 'Start of the observation window.',
  period_end_date DATE NOT NULL COMMENT 'End of the observation window.',
  observed_miles NUMBER(38,0) COMMENT 'Miles observed in the window.',
  mileage_band_code VARCHAR COMMENT 'Annualised mileage banding derived from observed miles.',
  harsh_braking_rate DECIMAL(6,3) COMMENT 'Harsh-braking events per 100 miles.',
  night_driving_pct DECIMAL(5,2) COMMENT 'Percentage of miles driven between 11pm and 5am.',
  telematics_score NUMBER(38,0) COMMENT 'Composite driving score 0-100; higher is safer.',
  source_system_code VARCHAR NOT NULL COMMENT 'Telematics provider or platform the aggregate originates from.'
)
COMMENT = 'Aggregated driving behaviour for a motor policy over a period, from a telematics device or app. Aggregates only - never raw journey traces - so the rating signal is usable without holding location-level personal data. Grain: One row per policy per observation period.';

ALTER TABLE BRICKSURANCE.enrichment.motor_telematics_aggregate ADD CONSTRAINT pk_motor_telematics_aggregate PRIMARY KEY (telematics_aggregate_id);
