-- Generated by tools/generate.py from bricksurance-data-core model v0.11.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.feature_set_member (
  feature_set_member_id VARCHAR NOT NULL COMMENT 'Identifier for the membership row.',
  feature_set_id VARCHAR NOT NULL COMMENT 'The feature set.',
  feature_id VARCHAR NOT NULL COMMENT 'The feature that is a member of the set.'
)
COMMENT = 'Membership of a feature in a feature set - the many-to-many bridge between feature_definition and feature_set. Modelled relationally so features are shared, not duplicated per set. Grain: One row per feature per feature set.';

ALTER TABLE BRICKSURANCE.model.feature_set_member ADD CONSTRAINT pk_feature_set_member PRIMARY KEY (feature_set_member_id);
