-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.party.consent (
  consent_id VARCHAR NOT NULL COMMENT 'Identifier for the consent record.',
  party_id VARCHAR NOT NULL COMMENT 'The consenting data subject.',
  consent_purpose_code VARCHAR NOT NULL COMMENT 'Purpose consented to.',
  consent_status_code VARCHAR NOT NULL COMMENT 'Current state of the consent.',
  granted_at TIMESTAMP_NTZ NOT NULL COMMENT 'When consent was given.',
  withdrawn_at TIMESTAMP_NTZ COMMENT 'When consent was withdrawn, where it was.',
  channel_code VARCHAR COMMENT 'Channel the consent was captured through.'
)
COMMENT = 'A data subject''s consent for a processing purpose. Withdrawal is a state change with a timestamp, never a deletion - the consent history is itself evidence. Grain: One row per party per purpose per consent grant.';

ALTER TABLE BRICKSURANCE.party.consent ADD CONSTRAINT pk_consent PRIMARY KEY (consent_id);
