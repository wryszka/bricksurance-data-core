-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.investment.instrument (
  instrument_id VARCHAR NOT NULL COMMENT 'Identifier for the instrument.',
  instrument_type_code VARCHAR NOT NULL COMMENT 'Kind of instrument.',
  issuer VARCHAR COMMENT 'Issuer name.',
  credit_rating_code VARCHAR COMMENT 'Credit quality banding.',
  coupon_rate DECIMAL(8,4) COMMENT 'Annual coupon rate for fixed income; empty for non-coupon assets.',
  maturity_date DATE COMMENT 'Redemption date for fixed income; empty for perpetual/equity/cash.',
  currency_code VARCHAR NOT NULL COMMENT 'Instrument currency.',
  ma_eligible BOOLEAN COMMENT 'Whether the instrument is eligible for a matching-adjustment portfolio.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the instrument is held in.'
)
COMMENT = 'An investment instrument the group can hold - its terms and credit quality. The reference detail behind holdings and positions; a stylised but real asset universe (standard-formula grade), not a full security master. Grain: One row per instrument.';

ALTER TABLE BRICKSURANCE.investment.instrument ADD CONSTRAINT pk_instrument PRIMARY KEY (instrument_id);
