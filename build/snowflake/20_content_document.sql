-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.content.document (
  document_id VARCHAR NOT NULL COMMENT 'Identifier for the document.',
  document_type_code VARCHAR NOT NULL COMMENT 'Kind of document.',
  title VARCHAR NOT NULL COMMENT 'Document title.',
  product_id VARCHAR COMMENT 'Product context (wordings attach here).',
  policy_id VARCHAR COMMENT 'Policy context (schedules attach here).',
  claim_id VARCHAR COMMENT 'Claim context (evidence attaches here).',
  submission_id VARCHAR COMMENT 'Submission context (slips attach here).',
  extracted_text VARCHAR COMMENT 'Full extracted text, indexed for semantic search.',
  volume_path VARCHAR COMMENT 'Path to the binary original in a governed Volume, where one exists.',
  created_date DATE NOT NULL COMMENT 'Date the document version was created.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'Governed unstructured content: policy wordings, schedules, claim evidence, survey reports, MRC slips. The extracted text is carried in the row so LLM tools search and cite documents under the same classification and lineage as structured data; binary originals live in a Volume referenced by path. Grain: One row per document version.';

ALTER TABLE BRICKSURANCE.content.document ADD CONSTRAINT pk_document PRIMARY KEY (document_id);
