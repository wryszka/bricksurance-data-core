-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.reinsurance.submission (
  submission_id VARCHAR NOT NULL COMMENT 'Stable identifier for the submission, owned by the data layer.',
  submission_reference VARCHAR NOT NULL COMMENT 'Submission reference as received; unique within a source system.',
  treaty_id VARCHAR COMMENT 'Treaty bound from this submission; populated only when BOUND.',
  submission_status_code VARCHAR NOT NULL COMMENT 'Lifecycle status from receipt to bind or decline.',
  treaty_type_code VARCHAR NOT NULL COMMENT 'Form of treaty requested.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Class of business the cover is requested for.',
  underwriting_year NUMBER(38,0) NOT NULL COMMENT 'Underwriting year the cover would attach to.',
  requested_limit DECIMAL(18,2) COMMENT 'Limit requested, in the submission currency.',
  requested_attachment DECIMAL(18,2) COMMENT 'Attachment point requested, for non-proportional forms.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the requested terms.',
  received_date DATE NOT NULL COMMENT 'Date the submission was received.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the submission originates from.'
)
COMMENT = 'A reinsurance submission: a cedant''s (or broker''s) request for treaty cover, from receipt through triage, pricing and decision. A bound submission links to the treaty it produced. Cedant and broker are party roles on the submission. Grain: One row per submission per source system.';

ALTER TABLE BRICKSURANCE.reinsurance.submission ADD CONSTRAINT pk_submission PRIMARY KEY (submission_id);
