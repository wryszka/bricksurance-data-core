-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.model_asset (
  model_id VARCHAR NOT NULL COMMENT 'Stable identifier for the model asset, owned by the data layer.',
  name VARCHAR NOT NULL COMMENT 'Human-readable model name.',
  model_type_code VARCHAR NOT NULL COMMENT 'The kind of model.',
  model_purpose_code VARCHAR NOT NULL COMMENT 'What the model is used for in the business.',
  line_of_business_code VARCHAR COMMENT 'Line of business the model applies to, where specific.',
  registered_name VARCHAR COMMENT 'Fully-qualified registry name, e.g. the Unity Catalog model name.',
  description VARCHAR COMMENT 'What the model does, in business language.',
  owner_role VARCHAR NOT NULL COMMENT 'Accountable owner of the model, as an org role.',
  source_system_code VARCHAR NOT NULL COMMENT 'Platform or registry the model is managed in.'
)
COMMENT = 'A governed analytical or machine-learning model as a business asset - what it is, what it is for, who owns it and where it is registered. Versions, features, scores and monitoring hang off it. This is how the semantic layer governs AI the way it governs data. Grain: One row per model asset.';

ALTER TABLE BRICKSURANCE.model.model_asset ADD CONSTRAINT pk_model_asset PRIMARY KEY (model_id);
