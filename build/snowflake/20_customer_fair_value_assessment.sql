-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.customer.fair_value_assessment (
  fair_value_assessment_id VARCHAR NOT NULL COMMENT 'Identifier for the assessment.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Product line assessed.',
  assessment_period VARCHAR NOT NULL COMMENT 'Period assessed, e.g. 2026-H1.',
  avg_premium DECIMAL(18,2) COMMENT 'Average earned premium per policy in the period.',
  avg_claims_paid DECIMAL(18,2) COMMENT 'Average claims incurred per policy in the period (the benefit).',
  claims_acceptance_rate DECIMAL(6,4) COMMENT 'Share of claims accepted (not declined).',
  avg_settlement_days NUMBER(38,0) COMMENT 'Average days from report to settlement.',
  complaint_ratio DECIMAL(8,4) COMMENT 'Complaints per 1,000 policies in the period.',
  fair_value_ratio DECIMAL(8,4) COMMENT 'Benefit-to-price ratio (claims incurred over earned premium).',
  fair_value_outcome_code VARCHAR NOT NULL COMMENT 'The fair-value verdict - fair, monitor or action.',
  attested_by VARCHAR COMMENT 'Role that attested the assessment.',
  attested_at DATE COMMENT 'Date of attestation.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the assessment is recorded in.'
)
COMMENT = 'A governed, attested Consumer Duty fair-value assessment for a product line in a period - price paid versus benefit delivered, with the verdict and who attested it. This is the board-level artifact: a versioned, owned, dated record, its numbers computed from premium and claims (see vw_fair_value_latest), not asserted. Grain: One row per line of business per assessment period.';

ALTER TABLE BRICKSURANCE.customer.fair_value_assessment ADD CONSTRAINT pk_fair_value_assessment PRIMARY KEY (fair_value_assessment_id);
