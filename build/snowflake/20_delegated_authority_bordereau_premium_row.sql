-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.delegated_authority.bordereau_premium_row (
  bordereau_premium_row_id VARCHAR NOT NULL COMMENT 'Identifier for the premium row.',
  submission_id VARCHAR NOT NULL COMMENT 'The premium bordereau this row belongs to.',
  risk_reference VARCHAR NOT NULL COMMENT 'Risk reference the premium relates to.',
  gross_premium DECIMAL(18,2) COMMENT 'Gross written premium in the period.',
  commission_amount DECIMAL(18,2) COMMENT 'Coverholder commission on the premium.',
  net_premium DECIMAL(18,2) COMMENT 'Premium net of commission, payable to the insurer.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amounts.'
)
COMMENT = 'A premium movement on a premium bordereau, mapped to Lloyd''s CDR premium fields. Grain: One row per premium movement per premium bordereau.';

ALTER TABLE BRICKSURANCE.delegated_authority.bordereau_premium_row ADD CONSTRAINT pk_bordereau_premium_row PRIMARY KEY (bordereau_premium_row_id);
