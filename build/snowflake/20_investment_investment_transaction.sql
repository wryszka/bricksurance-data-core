-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.investment.investment_transaction (
  investment_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the movement.',
  investment_holding_id VARCHAR NOT NULL COMMENT 'The holding moved.',
  investment_transaction_type_code VARCHAR NOT NULL COMMENT 'Kind of movement.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the holding currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Date of the movement.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A movement on an investment holding - purchase, sale, coupon, dividend or fair-value revaluation. Investment income and gains (the P&L line) and the movement in asset values (the balance sheet) are both derived from these. Grain: One row per investment movement.';

ALTER TABLE BRICKSURANCE.investment.investment_transaction ADD CONSTRAINT pk_investment_transaction PRIMARY KEY (investment_transaction_id);
