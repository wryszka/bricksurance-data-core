-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.model_score (
  model_score_id VARCHAR NOT NULL COMMENT 'Identifier for the score.',
  model_version_id VARCHAR NOT NULL COMMENT 'Model version that produced the score.',
  subject_kind_code VARCHAR NOT NULL COMMENT 'What kind of object the score is about.',
  subject_id VARCHAR NOT NULL COMMENT 'Identifier of the scored object; resolve against the table named by subject_kind_code (policy, quote, claim or party).',
  score DECIMAL(12,6) NOT NULL COMMENT 'The score value, in the model''s native scale.',
  scored_at TIMESTAMP_NTZ NOT NULL COMMENT 'When the score was produced.',
  source_system_code VARCHAR NOT NULL COMMENT 'Serving system that produced the score.'
)
COMMENT = 'A score a model version produced for a business object - the atomic output of a model. Polymorphic by subject_kind (policy, quote, claim, party) so one governed table holds every score and each traces to the version that made it. Business-facing signals such as a fraud signal consume scores; they are not the same thing. Grain: One row per scored subject per model version per scoring run.';

ALTER TABLE BRICKSURANCE.model.model_score ADD CONSTRAINT pk_model_score PRIMARY KEY (model_score_id);
