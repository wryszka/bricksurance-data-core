-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim_transaction (
  claim_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the claim transaction.',
  claim_id VARCHAR NOT NULL COMMENT 'Claim the movement belongs to.',
  claim_transaction_type_code VARCHAR NOT NULL COMMENT 'Kind of financial movement.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the transaction currency. Reserve increases positive, reserve releases negative; payments positive; recoveries negative from the insurer''s cost perspective.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Date the movement was booked.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the movement originates from.'
)
COMMENT = 'A signed financial movement on a claim: case reserve movements, indemnity and expense payments, and recoveries. Outstanding, paid and incurred are always derived by summation over these movements, never stored as balances. Grain: One row per financial movement per claim.';

ALTER TABLE BRICKSURANCE.claim.claim_transaction ADD CONSTRAINT pk_claim_transaction PRIMARY KEY (claim_transaction_id);
