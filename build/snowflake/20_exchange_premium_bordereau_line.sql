-- Generated by tools/generate.py from bricksurance-data-core model v0.5.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.exchange.premium_bordereau_line (
  bordereau_line_id VARCHAR NOT NULL COMMENT 'Identifier for the bordereau line, assigned on ingestion.',
  reporting_month DATE NOT NULL COMMENT 'First day of the calendar month the bordereau reports on.',
  coverholder_name VARCHAR NOT NULL COMMENT 'Name of the coverholder or broker submitting the bordereau.',
  policy_number VARCHAR NOT NULL COMMENT 'Policy reference as reported by the coverholder; matched to the core model downstream.',
  insured_name VARCHAR COMMENT 'Name of the insured as reported.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business, mapped from the coverholder''s own class descriptions to the canonical code set.',
  inception_date DATE NOT NULL COMMENT 'Cover inception as reported.',
  expiry_date DATE COMMENT 'Cover expiry as reported.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the reported amounts.',
  gross_premium DECIMAL(18,2) NOT NULL COMMENT 'Gross written premium reported for the line.',
  commission_amount DECIMAL(18,2) COMMENT 'Coverholder commission reported for the line.',
  risk_country_code VARCHAR COMMENT 'Country of the risk location, where reported.',
  risk_postcode VARCHAR COMMENT 'Postal code of the risk location, where reported.',
  source_system_code VARCHAR NOT NULL COMMENT 'Provenance of the line; coverholder bordereaux use COVERHOLDER_BDX.'
)
COMMENT = 'The canonical form of an inbound premium bordereau line, as reported by a coverholder or broker. Messy source files land in the exchange domain and are mapped into this shape - using the data dictionary as the contract - before flowing into the core model. Grain: One row per policy per reporting month per inbound bordereau.';

ALTER TABLE BRICKSURANCE.exchange.premium_bordereau_line ADD CONSTRAINT pk_premium_bordereau_line PRIMARY KEY (bordereau_line_id);
