-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.life.assumption_set (
  assumption_set_id VARCHAR NOT NULL COMMENT 'Identifier for the assumption set version.',
  assumption_type_code VARCHAR NOT NULL COMMENT 'Kind of assumption governed by this set.',
  assumption_status_code VARCHAR NOT NULL COMMENT 'Maker/checker lifecycle status.',
  version NUMBER(38,0) NOT NULL COMMENT 'Monotonic version number within the assumption type.',
  description VARCHAR COMMENT 'What changed in this version and why.',
  effective_from DATE COMMENT 'First valuation date this set applies to, once approved.',
  approved_by VARCHAR COMMENT 'Checker who approved or rejected the set.',
  approved_at TIMESTAMP_NTZ COMMENT 'When the approval decision was made.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the assumption set originates from.'
)
COMMENT = 'A versioned, governed set of actuarial assumptions (mortality, lapse, expense, economic). Sets move through a maker/checker lifecycle; valuation runs record exactly which approved set they used, which is what makes any past result reproducible. Grain: One row per assumption set version per assumption type.';

ALTER TABLE BRICKSURANCE.life.assumption_set ADD CONSTRAINT pk_assumption_set PRIMARY KEY (assumption_set_id);
