-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.investment.position (
  position_id VARCHAR NOT NULL COMMENT 'Identifier for the position row.',
  portfolio_code VARCHAR NOT NULL COMMENT 'Portfolio the position sits in.',
  instrument_id VARCHAR NOT NULL COMMENT 'Instrument held.',
  as_of_month DATE NOT NULL COMMENT 'Month-end the position is struck at.',
  nominal DECIMAL(18,2) COMMENT 'Nominal / face amount held, in instrument currency.',
  market_value DECIMAL(18,2) NOT NULL COMMENT 'Market value of the holding, in instrument currency.',
  book_value DECIMAL(18,2) COMMENT 'Amortised book value of the holding, in instrument currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the values.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the position is held in.'
)
COMMENT = 'The holding of an instrument in a portfolio as at a month-end - nominal, market and book value. The time series that duration, yield and stress are computed over. Grain: One row per portfolio per instrument per as-of month.';

ALTER TABLE BRICKSURANCE.investment.position ADD CONSTRAINT pk_position PRIMARY KEY (position_id);
