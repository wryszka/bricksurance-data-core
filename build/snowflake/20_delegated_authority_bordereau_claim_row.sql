-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.delegated_authority.bordereau_claim_row (
  bordereau_claim_row_id VARCHAR NOT NULL COMMENT 'Identifier for the claim row.',
  submission_id VARCHAR NOT NULL COMMENT 'The claims bordereau this row belongs to.',
  risk_reference VARCHAR NOT NULL COMMENT 'Risk reference the claim relates to.',
  claim_reference VARCHAR NOT NULL COMMENT 'Coverholder''s claim reference.',
  cause_of_loss_code VARCHAR COMMENT 'Peril / cause of loss.',
  loss_date DATE COMMENT 'Date of loss.',
  paid_amount DECIMAL(18,2) COMMENT 'Amount paid in the period.',
  outstanding_amount DECIMAL(18,2) COMMENT 'Outstanding reserve at period end.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amounts.'
)
COMMENT = 'A claim movement on a claims bordereau, mapped to Lloyd''s CDR claims fields. Grain: One row per claim movement per claims bordereau.';

ALTER TABLE BRICKSURANCE.delegated_authority.bordereau_claim_row ADD CONSTRAINT pk_bordereau_claim_row PRIMARY KEY (bordereau_claim_row_id);
