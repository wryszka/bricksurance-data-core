-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.fraud_signal (
  fraud_signal_id VARCHAR NOT NULL COMMENT 'Identifier for the signal.',
  claim_id VARCHAR NOT NULL COMMENT 'Claim the signal concerns.',
  fraud_signal_type_code VARCHAR NOT NULL COMMENT 'Kind of indicator.',
  score NUMBER(38,0) NOT NULL COMMENT 'Signal strength 0-100.',
  detected_at TIMESTAMP_NTZ NOT NULL COMMENT 'When the signal was raised.',
  detail VARCHAR COMMENT 'What was observed, in business language.',
  source_system_code VARCHAR NOT NULL COMMENT 'Detecting system.'
)
COMMENT = 'A fraud indicator raised on a claim, with its score and provenance. Signals inform SIU referral decisions; a signal is never itself a decision. Grain: One row per signal per claim.';

ALTER TABLE BRICKSURANCE.claim.fraud_signal ADD CONSTRAINT pk_fraud_signal PRIMARY KEY (fraud_signal_id);
