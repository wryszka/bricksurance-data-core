-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.contract_group (
  contract_group_id VARCHAR NOT NULL COMMENT 'Identifier for the group of contracts.',
  legal_entity_id VARCHAR NOT NULL COMMENT 'The carrier holding the group.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business of the group.',
  cohort_year NUMBER(38,0) NOT NULL COMMENT 'Annual cohort (issue year) - contracts more than a year apart cannot share a group.',
  measurement_model_code VARCHAR NOT NULL COMMENT 'IFRS 17 model applied (GMM, PAA, VFA).',
  onerous BOOLEAN NOT NULL COMMENT 'Whether the group is onerous at initial recognition.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the group''s measurement.'
)
COMMENT = 'An IFRS 17 group of contracts - the unit of account for the standard: contracts of similar risk, managed together, issued within a year, split by onerous status. Carries the measurement model and links to the entity and line. The CSM roll-forward and the LRC/LIC split hang off this. Grain: One row per group of contracts.';

ALTER TABLE BRICKSURANCE.finance.contract_group ADD CONSTRAINT pk_contract_group PRIMARY KEY (contract_group_id);
