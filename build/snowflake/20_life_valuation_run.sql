-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.life.valuation_run (
  valuation_run_id VARCHAR NOT NULL COMMENT 'Identifier for the run.',
  legal_entity_id VARCHAR COMMENT 'The legal entity the run values. With reporting_level this is what distinguishes a subsidiary''s solo run from the group consolidated run.',
  reporting_level_code VARCHAR COMMENT 'Solo (this entity) or group (consolidated).',
  reporting_regime_code VARCHAR COMMENT 'Regime the run is prepared under (Solvency II, IFRS 17...).',
  valuation_date DATE NOT NULL COMMENT 'Valuation date the run values the book at.',
  run_timestamp TIMESTAMP_NTZ NOT NULL COMMENT 'When the run executed.',
  scenario_set_id VARCHAR COMMENT 'Scenario set the run used, for stochastic runs.',
  model_version VARCHAR COMMENT 'Version of the valuation model/engine used (e.g. a Unity Catalog model version).',
  run_verdict_code VARCHAR NOT NULL COMMENT 'Quality-gate verdict; RED runs never feed published results.',
  run_by VARCHAR COMMENT 'Operator or service principal that executed the run.',
  source_system_code VARCHAR NOT NULL COMMENT 'System that executed the run.'
)
COMMENT = 'One execution of a valuation process: which valuation date, which approved assumption sets (one per assumption type, via valuation_run_assumption), which scenario set, which model version, run by whom, with what quality verdict. The run is the unit of auditability - every published valuation result points back to exactly one run. Grain: One row per valuation run execution.';

ALTER TABLE BRICKSURANCE.life.valuation_run ADD CONSTRAINT pk_valuation_run PRIMARY KEY (valuation_run_id);
