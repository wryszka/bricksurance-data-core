-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.feature_definition (
  feature_id VARCHAR NOT NULL COMMENT 'Identifier for the feature.',
  name VARCHAR NOT NULL COMMENT 'Feature name as referenced by models.',
  description VARCHAR NOT NULL COMMENT 'What the feature represents, in business language.',
  source_entity VARCHAR COMMENT 'The model entity the feature is derived from, e.g. motor_telematics_aggregate.',
  expression VARCHAR COMMENT 'Definition or derivation of the feature, as documentation.',
  classification VARCHAR NOT NULL COMMENT 'Data classification inherited by the feature - internal, confidential or pii.',
  pii_flag BOOLEAN NOT NULL COMMENT 'Whether the feature is built from personal data.'
)
COMMENT = 'A single model feature defined once - what it means, where it comes from and how it is classified. Features are governed the same way attributes are: a feature built from personal data carries that classification wherever it is used. Grain: One row per feature.';

ALTER TABLE BRICKSURANCE.model.feature_definition ADD CONSTRAINT pk_feature_definition PRIMARY KEY (feature_id);
