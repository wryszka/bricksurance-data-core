-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.model_version (
  model_version_id VARCHAR NOT NULL COMMENT 'Identifier for this model version.',
  model_id VARCHAR NOT NULL COMMENT 'Model asset this is a version of.',
  version VARCHAR NOT NULL COMMENT 'Version label, e.g. 3 or 2026.07.1.',
  model_status_code VARCHAR NOT NULL COMMENT 'Serving role of this version - champion, challenger, shadow, retired, draft.',
  trained_on DATE COMMENT 'Date the version was trained.',
  training_frame_ref VARCHAR COMMENT 'Reference to the governed training frame or view the version was trained on.',
  primary_metric_type_code VARCHAR COMMENT 'The headline evaluation metric reported at training time.',
  primary_metric_value DECIMAL(12,4) COMMENT 'Value of the headline evaluation metric at training time.',
  promoted_on DATE COMMENT 'Date this version became champion, where applicable.',
  source_system_code VARCHAR NOT NULL COMMENT 'Registry the version is tracked in.'
)
COMMENT = 'A specific trained version of a model asset - when it was trained, on what frame, how it scored, and its serving role. Every score and decision cites a version, so a decision is always reproducible to the exact model that made it. Grain: One row per version of a model asset.';

ALTER TABLE BRICKSURANCE.model.model_version ADD CONSTRAINT pk_model_version PRIMARY KEY (model_version_id);
