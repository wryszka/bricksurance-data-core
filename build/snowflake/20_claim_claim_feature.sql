-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim_feature (
  claim_feature_id VARCHAR NOT NULL COMMENT 'Identifier for the claim feature.',
  claim_id VARCHAR NOT NULL COMMENT 'The claim this feature belongs to.',
  coverage_id VARCHAR COMMENT 'Coverage the feature attaches to, where the source provides it.',
  claim_feature_type_code VARCHAR NOT NULL COMMENT 'Kind of feature (head of damage).',
  claim_status_code VARCHAR NOT NULL COMMENT 'Handling status of the feature; features can close independently.',
  source_system_code VARCHAR NOT NULL COMMENT 'Claims system the feature originates from.'
)
COMMENT = 'A feature (head of damage) within a claim - a sub-claim that reserves and settles on its own, e.g. own damage versus third-party injury under one motor claim. Financial development still flows through claim transactions; the feature gives claims and reserving teams the grain they actually work at. Grain: One row per feature per claim.';

ALTER TABLE BRICKSURANCE.claim.claim_feature ADD CONSTRAINT pk_claim_feature PRIMARY KEY (claim_feature_id);
