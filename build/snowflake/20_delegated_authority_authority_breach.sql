-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.delegated_authority.authority_breach (
  authority_breach_id VARCHAR NOT NULL COMMENT 'Identifier for the breach.',
  binder_id VARCHAR NOT NULL COMMENT 'The binder breached.',
  bordereau_risk_row_id VARCHAR COMMENT 'The risk row that breached, where applicable.',
  breach_type_code VARCHAR NOT NULL COMMENT 'Kind of breach.',
  detected_by VARCHAR COMMENT 'The detector that raised it (e.g. vw_authority_breach / a governed function).',
  detail VARCHAR COMMENT 'What was observed, in business language.',
  breach_status_code VARCHAR NOT NULL COMMENT 'Open, waived or remediated.',
  waived_by VARCHAR COMMENT 'Role that waived the breach under maker/checker, where waived.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the breach record is held in.'
)
COMMENT = 'A breach of a binder''s authority detected on a bordereau risk - the governed record that carries the waiver. Detection is deterministic (see vw_authority_breach); the waiver is maker/checker, with the approver recorded. AI narrates; it never decides. Grain: One row per detected breach.';

ALTER TABLE BRICKSURANCE.delegated_authority.authority_breach ADD CONSTRAINT pk_authority_breach PRIMARY KEY (authority_breach_id);
