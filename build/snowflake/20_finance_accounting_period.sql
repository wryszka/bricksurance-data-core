-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.accounting_period (
  accounting_period_id VARCHAR NOT NULL COMMENT 'Identifier for the period.',
  legal_entity_id VARCHAR NOT NULL COMMENT 'The entity whose books this period governs.',
  period_label VARCHAR NOT NULL COMMENT 'Human label, e.g. 2026-Q2.',
  start_date DATE NOT NULL COMMENT 'First day of the period.',
  end_date DATE NOT NULL COMMENT 'Last day of the period.',
  period_status_code VARCHAR NOT NULL COMMENT 'Open, closed or locked.'
)
COMMENT = 'A financial reporting period for a legal entity, with its open/closed/locked status. Postings are only permitted into open periods; the close process moves periods to locked. The unit the finance calendar runs on. Grain: One row per period per legal entity.';

ALTER TABLE BRICKSURANCE.finance.accounting_period ADD CONSTRAINT pk_accounting_period PRIMARY KEY (accounting_period_id);
