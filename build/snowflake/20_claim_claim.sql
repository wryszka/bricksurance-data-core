-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim (
  claim_id VARCHAR NOT NULL COMMENT 'Stable, source-agnostic identifier for the claim, owned by the data layer.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy the claim is made under.',
  coverage_id VARCHAR COMMENT 'Coverage the claim attaches to, where the source provides it.',
  claim_number VARCHAR NOT NULL COMMENT 'Claim number as issued by the claims system. Unique within a source system - always interpret together with source_system_code.',
  claim_status_code VARCHAR NOT NULL COMMENT 'Current handling status of the claim.',
  cause_of_loss_code VARCHAR NOT NULL COMMENT 'Primary peril or event that gave rise to the claim.',
  loss_date DATE NOT NULL COMMENT 'Date the loss occurred.',
  reported_date DATE NOT NULL COMMENT 'Date the loss was first reported to the insurer. Must be on or after loss_date.',
  description VARCHAR COMMENT 'Free-text description of the loss circumstances.',
  handling_office VARCHAR COMMENT 'Claims office or team handling the file.',
  catastrophe_id VARCHAR COMMENT 'Catastrophe event the claim is attributed to, where it belongs to one.',
  reopened_flag BOOLEAN COMMENT 'Whether the claim has been reopened after closure.',
  policy_version_id VARCHAR COMMENT 'The policy version in force at the loss date - claims attach to the as-at version, not just the policy, so cover is read as it stood at loss.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the claim originates from.'
)
COMMENT = 'A demand for indemnity under a policy arising from a loss event. The claim record holds the event and handling state; all financial development lives in claim transactions. Grain: One row per claim per source system.';

ALTER TABLE BRICKSURANCE.claim.claim ADD CONSTRAINT pk_claim PRIMARY KEY (claim_id);
