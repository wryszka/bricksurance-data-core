-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy_lifecycle.renewal_chain (
  renewal_chain_id VARCHAR NOT NULL COMMENT 'Identifier for the chain-membership row.',
  policy_id VARCHAR NOT NULL COMMENT 'The policy placed in the chain.',
  chain_id VARCHAR NOT NULL COMMENT 'Stable identifier of the risk across all its renewals.',
  predecessor_policy_id VARCHAR COMMENT 'The prior policy this one renewed from; empty for the first term.',
  renewal_number NUMBER(38,0) NOT NULL COMMENT 'Position in the chain; 0 for new business, 1 for first renewal, and so on.'
)
COMMENT = 'Links a policy to its predecessor and to the stable chain of all renewals of one original risk. This is what makes "customer since 2019, seventh renewal" answerable, and what the renewal loop and the customer dimension hang off. It formalises the policy renews-from linkage as first-class data. Grain: One row per policy, placing it in its renewal chain.';

ALTER TABLE BRICKSURANCE.policy_lifecycle.renewal_chain ADD CONSTRAINT pk_renewal_chain PRIMARY KEY (renewal_chain_id);
