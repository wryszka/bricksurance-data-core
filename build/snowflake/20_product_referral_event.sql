-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.referral_event (
  referral_event_id VARCHAR NOT NULL COMMENT 'Identifier for the referral event.',
  referral_rule_id VARCHAR NOT NULL COMMENT 'The exact rule version that fired.',
  subject_kind_code VARCHAR NOT NULL COMMENT 'Whether the rule fired on a submission or a quote.',
  subject_id VARCHAR NOT NULL COMMENT 'Identifier of the submission or quote the rule fired on.',
  fired_at TIMESTAMP_NTZ NOT NULL COMMENT 'When the rule fired.',
  referral_outcome_code VARCHAR NOT NULL COMMENT 'How the referral was resolved.',
  decided_by VARCHAR COMMENT 'Underwriter role that resolved the referral, where a human did.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the event originates from.'
)
COMMENT = 'A firing of a referral rule against a risk - the telemetry of the rulebook. Each event cites the exact rule version that fired and how it was resolved, so the rulebook can be audited: which rules fire, on what, and whether underwriters uphold or override them. Grain: One row per rule firing against a submission or quote.';

ALTER TABLE BRICKSURANCE.product.referral_event ADD CONSTRAINT pk_referral_event PRIMARY KEY (referral_event_id);
