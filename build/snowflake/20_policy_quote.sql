-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy.quote (
  quote_id VARCHAR NOT NULL COMMENT 'Stable, source-agnostic identifier for the quote, owned by the data layer.',
  quote_number VARCHAR NOT NULL COMMENT 'Quote reference as issued; unique within a source system.',
  policy_id VARCHAR COMMENT 'Policy issued from this quote; populated only when the quote converted.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business quoted.',
  quote_status_code VARCHAR NOT NULL COMMENT 'Lifecycle status of the quote.',
  quote_date DATE NOT NULL COMMENT 'Date the quotation was produced.',
  requested_inception_date DATE COMMENT 'Cover start date requested by the prospect.',
  quoted_gross_premium DECIMAL(18,2) COMMENT 'Gross premium quoted, in the quote currency; empty while the quote is open.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the quoted premium.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the quote originates from.',
  product_id VARCHAR COMMENT 'The product quoted, where the product catalog applies.',
  distribution_channel_code VARCHAR COMMENT 'Channel the quote was requested through.'
)
COMMENT = 'A quotation for insurance cover. Quotes live upstream of policies: a converted quote links to the policy issued from it, closing the quote-to-policy thread that pricing and underwriting processes work in. Premium only ever arises on the policy. Grain: One row per quotation per source system.';

ALTER TABLE BRICKSURANCE.policy.quote ADD CONSTRAINT pk_quote PRIMARY KEY (quote_id);
