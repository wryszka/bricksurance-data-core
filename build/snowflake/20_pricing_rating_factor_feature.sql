-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.pricing.rating_factor_feature (
  rating_factor_feature_id VARCHAR NOT NULL COMMENT 'Identifier for the link row.',
  rating_factor_id VARCHAR NOT NULL COMMENT 'The rating factor.',
  feature_id VARCHAR NOT NULL COMMENT 'The model feature that supplies the factor.'
)
COMMENT = 'The link between a rating factor and the model feature that supplies it - the bridge from the rating structure to the governed feature catalog. It is what lets a factor''s data classification and lineage flow from the feature it uses. Grain: One row per rating factor per feature it draws on.';

ALTER TABLE BRICKSURANCE.pricing.rating_factor_feature ADD CONSTRAINT pk_rating_factor_feature PRIMARY KEY (rating_factor_feature_id);
