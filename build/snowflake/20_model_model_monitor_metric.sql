-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.model_monitor_metric (
  model_monitor_metric_id VARCHAR NOT NULL COMMENT 'Identifier for the monitoring measurement.',
  model_version_id VARCHAR NOT NULL COMMENT 'Model version the measurement concerns.',
  monitor_metric_type_code VARCHAR NOT NULL COMMENT 'The kind of metric measured.',
  period VARCHAR NOT NULL COMMENT 'Period the measurement covers, e.g. 2026-Q2.',
  value DECIMAL(12,4) NOT NULL COMMENT 'Measured value of the metric.',
  breach_flag BOOLEAN COMMENT 'Whether the value breached its governance threshold in the period.',
  source_system_code VARCHAR NOT NULL COMMENT 'Monitoring system the measurement originates from.'
)
COMMENT = 'A monitoring measurement for a model version over a period - performance and drift tracked through time. This is what turns "the model is governed" into evidence: whether the champion still earns its role. Grain: One row per model version per metric type per period.';

ALTER TABLE BRICKSURANCE.model.model_monitor_metric ADD CONSTRAINT pk_model_monitor_metric PRIMARY KEY (model_monitor_metric_id);
