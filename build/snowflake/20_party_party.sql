-- Generated by tools/generate.py from bricksurance-data-core model v0.5.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.party.party (
  party_id VARCHAR NOT NULL COMMENT 'Stable, source-agnostic identifier for the party, owned by the data layer.',
  party_type_code VARCHAR NOT NULL COMMENT 'Whether the party is a natural person or a legal entity.',
  name VARCHAR NOT NULL COMMENT 'Full legal name of the party.',
  country_code VARCHAR NOT NULL COMMENT 'Country of residence or registration.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record this party was mastered from.'
)
COMMENT = 'A person or organisation the group deals with. A party exists exactly once regardless of how many relationships it has; what the party does (insured, broker, cedant, claimant...) is expressed through party roles, never by duplicating the party. Grain: One row per unique legal person or organisation, mastered across source systems.';

ALTER TABLE BRICKSURANCE.party.party ADD CONSTRAINT pk_party PRIMARY KEY (party_id);
