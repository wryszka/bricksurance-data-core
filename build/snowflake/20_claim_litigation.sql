-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.litigation (
  litigation_id VARCHAR NOT NULL COMMENT 'Identifier for the litigation matter.',
  claim_id VARCHAR NOT NULL COMMENT 'The claim the proceedings arise from.',
  litigation_status_code VARCHAR NOT NULL COMMENT 'Status of the proceedings.',
  jurisdiction VARCHAR COMMENT 'Court or jurisdiction the matter is in.',
  legal_reserve DECIMAL(18,2) COMMENT 'Reserve held for legal and defence costs, in the claim currency.',
  currency_code VARCHAR COMMENT 'Currency of the legal reserve.',
  opened_date DATE COMMENT 'Date proceedings were first notified.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the litigation record originates from.'
)
COMMENT = 'Legal proceedings arising from a claim - status, jurisdiction and the legal reserve. Litigation is a material driver of claim cost and duration; modelling it lets the claims workbench track defended matters distinctly. Grain: One row per suit per claim.';

ALTER TABLE BRICKSURANCE.claim.litigation ADD CONSTRAINT pk_litigation PRIMARY KEY (litigation_id);
