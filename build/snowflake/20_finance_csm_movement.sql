-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.csm_movement (
  csm_movement_id VARCHAR NOT NULL COMMENT 'Identifier for the movement.',
  contract_group_id VARCHAR NOT NULL COMMENT 'The group of contracts.',
  valuation_run_id VARCHAR NOT NULL COMMENT 'The run that produced this movement - the audit anchor.',
  csm_movement_type_code VARCHAR NOT NULL COMMENT 'Which step of the roll-forward this is.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the group currency; release is negative.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.'
)
COMMENT = 'A step in the contractual service margin roll-forward for a group of contracts in a period: opening, new business, interest accretion, experience adjustments and release to profit. The closing CSM is the signed sum over the opening - a balance derived from movements, never overwritten, and produced by an auditable valuation run. Grain: One row per movement type per contract group per period.';

ALTER TABLE BRICKSURANCE.finance.csm_movement ADD CONSTRAINT pk_csm_movement PRIMARY KEY (csm_movement_id);
