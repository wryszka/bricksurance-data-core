-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.investment.asset_cashflow (
  asset_cashflow_id VARCHAR NOT NULL COMMENT 'Identifier for the cashflow.',
  instrument_id VARCHAR NOT NULL COMMENT 'Instrument the cashflow belongs to.',
  cashflow_month DATE NOT NULL COMMENT 'Month the cashflow occurs in.',
  coupon_amount DECIMAL(18,2) COMMENT 'Coupon paid in the month, per unit nominal held (portfolio-scaled downstream).',
  redemption_amount DECIMAL(18,2) COMMENT 'Principal redeemed in the month, per unit nominal.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the cashflow.'
)
COMMENT = 'A projected future cashflow of an instrument - coupon and/or redemption in a month. The input to duration, matching and ALM: the asset leg the annuity liability cashflows are matched against. Grain: One row per instrument per future cashflow month.';

ALTER TABLE BRICKSURANCE.investment.asset_cashflow ADD CONSTRAINT pk_asset_cashflow PRIMARY KEY (asset_cashflow_id);
