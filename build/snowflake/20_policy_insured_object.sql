-- Generated by tools/generate.py from bricksurance-data-core model v0.5.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy.insured_object (
  insured_object_id VARCHAR NOT NULL COMMENT 'Identifier for the insured object.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy the object is insured under.',
  insured_object_type_code VARCHAR NOT NULL COMMENT 'Kind of object or exposure insured.',
  description VARCHAR COMMENT 'Human-readable description of the object, as declared.',
  country_code VARCHAR NOT NULL COMMENT 'Country where the object is located or the exposure arises.',
  postcode VARCHAR COMMENT 'Postal code of the object''s location, where applicable; drives geographic accumulation.'
)
COMMENT = 'A concrete object or exposure insured under a policy - a building, a vehicle, a shipment, a set of operations. Location lives here, which makes this the anchor for exposure and accumulation analysis. Grain: One row per insured object under a policy.';

ALTER TABLE BRICKSURANCE.policy.insured_object ADD CONSTRAINT pk_insured_object PRIMARY KEY (insured_object_id);
