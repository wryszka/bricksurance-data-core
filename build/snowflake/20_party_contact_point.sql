-- Generated by tools/generate.py from bricksurance-data-core model v0.11.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.party.contact_point (
  contact_point_id VARCHAR NOT NULL COMMENT 'Identifier for the contact point.',
  party_id VARCHAR NOT NULL COMMENT 'The party contactable here.',
  contact_point_type_code VARCHAR NOT NULL COMMENT 'Kind of contact point.',
  value VARCHAR NOT NULL COMMENT 'The address, number or email itself.',
  preferred BOOLEAN NOT NULL COMMENT 'Whether this is the party''s preferred contact point of its kind.'
)
COMMENT = 'A contact point for a party. PII is deliberately concentrated here and on party - the erasure design in PRIVACY.md redacts these anchors while transactional history keeps its surrogate keys. Grain: One row per contact point per party.';

ALTER TABLE BRICKSURANCE.party.contact_point ADD CONSTRAINT pk_contact_point PRIMARY KEY (contact_point_id);
