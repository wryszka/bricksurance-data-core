-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.journal (
  journal_id VARCHAR NOT NULL COMMENT 'Identifier for the journal.',
  journal_reference VARCHAR NOT NULL COMMENT 'Human reference for the journal.',
  legal_entity_id VARCHAR NOT NULL COMMENT 'The entity whose ledger this posts to.',
  accounting_period_id VARCHAR NOT NULL COMMENT 'The period the journal posts into; must be open at posting time.',
  posting_date DATE NOT NULL COMMENT 'Ledger date of the entry.',
  description VARCHAR COMMENT 'What the entry records.',
  source_system_code VARCHAR NOT NULL COMMENT 'System that raised the journal.'
)
COMMENT = 'A journal entry header: a balanced set of debit and credit lines posted to one legal entity in one period. Every journal''s lines sum to zero - that invariant is what makes this a real double-entry ledger rather than a pile of classified amounts. Grain: One row per journal entry.';

ALTER TABLE BRICKSURANCE.finance.journal ADD CONSTRAINT pk_journal PRIMARY KEY (journal_id);
