-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.distribution.commission_transaction (
  commission_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the movement.',
  policy_id VARCHAR NOT NULL COMMENT 'The policy the commission relates to.',
  party_id VARCHAR NOT NULL COMMENT 'The intermediary earning (or repaying) the commission.',
  commission_type_code VARCHAR NOT NULL COMMENT 'Kind of remuneration.',
  rate DECIMAL(5,4) COMMENT 'Commission rate applied, as a fraction of premium.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the transaction currency; clawbacks negative.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Date the movement was booked.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A signed intermediary remuneration movement on a policy. Transactional like all money in this model; disclosable commission is a query, not a project. Grain: One row per commission movement per policy per intermediary.';

ALTER TABLE BRICKSURANCE.distribution.commission_transaction ADD CONSTRAINT pk_commission_transaction PRIMARY KEY (commission_transaction_id);
