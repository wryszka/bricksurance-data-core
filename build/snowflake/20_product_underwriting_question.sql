-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.underwriting_question (
  underwriting_question_id VARCHAR NOT NULL COMMENT 'Identifier for the question.',
  product_id VARCHAR NOT NULL COMMENT 'The product asking the question.',
  question_order NUMBER(38,0) NOT NULL COMMENT 'Presentation order.',
  question_text VARCHAR NOT NULL COMMENT 'The question as asked.',
  answer_type VARCHAR NOT NULL COMMENT 'Expected answer type - boolean, number, text or a code.',
  required BOOLEAN NOT NULL COMMENT 'Whether an answer is mandatory to quote.'
)
COMMENT = 'A question the product asks at quotation. The question set is data, so any channel - a form, an aggregator API or a buyer agent - can discover what the product needs to know and answer it programmatically. Grain: One row per question per product.';

ALTER TABLE BRICKSURANCE.product.underwriting_question ADD CONSTRAINT pk_underwriting_question PRIMARY KEY (underwriting_question_id);
