-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.valuation_result (
  valuation_result_id VARCHAR NOT NULL COMMENT 'Identifier for the result row.',
  valuation_run_id VARCHAR NOT NULL COMMENT 'The run that produced this figure.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the figure relates to.',
  valuation_measure_code VARCHAR NOT NULL COMMENT 'Which defined measure this figure is.',
  cohort VARCHAR COMMENT 'Optional sub-division (e.g. an age-by-term band or underwriting-year cohort).',
  amount DECIMAL(18,2) NOT NULL COMMENT 'The figure, in the stated currency. Sign follows the measure''s own convention.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.'
)
COMMENT = 'A published valuation figure: one measure (BEL, technical provision, IBNR, SCR, CSM...) for one line of business and cohort, produced by exactly one auditable valuation run. One shape serves life and non-life, Solvency II and IFRS 17 - new regimes add measure codes, not tables. Grain: One row per valuation run per line of business per measure per cohort.';

ALTER TABLE BRICKSURANCE.finance.valuation_result ADD CONSTRAINT pk_valuation_result PRIMARY KEY (valuation_result_id);
