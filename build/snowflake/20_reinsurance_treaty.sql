-- Generated by tools/generate.py from bricksurance-data-core model v0.4.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.reinsurance.treaty (
  treaty_id VARCHAR NOT NULL COMMENT 'Stable identifier for the treaty, owned by the data layer.',
  treaty_reference VARCHAR NOT NULL COMMENT 'Treaty reference as agreed between the parties.',
  treaty_type_code VARCHAR NOT NULL COMMENT 'Form of the treaty.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Class of business the treaty covers.',
  underwriting_year NUMBER(38,0) NOT NULL COMMENT 'Underwriting year of account of the treaty.',
  inception_date DATE NOT NULL COMMENT 'Date treaty cover begins (inclusive).',
  expiry_date DATE NOT NULL COMMENT 'Date treaty cover ends (inclusive). Must be on or after inception_date.',
  cession_rate DECIMAL(5,4) COMMENT 'Contractual ceded share for proportional treaties, as a fraction between 0 and 1; empty for non-proportional forms.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the treaty.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the treaty originates from.'
)
COMMENT = 'A treaty reinsurance contract under which risks are ceded to or assumed from reinsurers. Cedant and reinsurer are party roles on the treaty; which business it covers is expressed through cessions. Grain: One row per treaty contract per underwriting year.';

ALTER TABLE BRICKSURANCE.reinsurance.treaty ADD CONSTRAINT pk_treaty PRIMARY KEY (treaty_id);
