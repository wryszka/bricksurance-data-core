-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.expense_transaction (
  expense_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the expense movement.',
  legal_entity_id VARCHAR COMMENT 'The entity the expense is booked in.',
  expense_type_code VARCHAR NOT NULL COMMENT 'Kind of expense.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line the expense is allocated to.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the stated currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Booking date.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'An operating expense allocated to a line of business - the missing half of the combined ratio, transactional like all money in the model. Grain: One row per expense allocation per line per period booking.';

ALTER TABLE BRICKSURANCE.finance.expense_transaction ADD CONSTRAINT pk_expense_transaction PRIMARY KEY (expense_transaction_id);
