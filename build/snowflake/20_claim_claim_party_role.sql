-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim_party_role (
  claim_party_role_id VARCHAR NOT NULL COMMENT 'Identifier for the claim party role.',
  claim_id VARCHAR NOT NULL COMMENT 'The claim the party is involved in.',
  party_id VARCHAR NOT NULL COMMENT 'The party playing the role.',
  claim_party_role_type_code VARCHAR NOT NULL COMMENT 'The role the party plays on the claim.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the involvement originates from.'
)
COMMENT = 'A party and the role it plays on a specific claim - handler, adjuster, claimant, third party, solicitor or supplier. Distinct from policy-level party roles: it captures who is involved in this loss, not the contract. Grain: One row per party per role per claim.';

ALTER TABLE BRICKSURANCE.claim.claim_party_role ADD CONSTRAINT pk_claim_party_role PRIMARY KEY (claim_party_role_id);
