-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim_reserve_category (
  claim_reserve_category_id VARCHAR NOT NULL COMMENT 'Identifier for the reserve category row.',
  claim_feature_id VARCHAR NOT NULL COMMENT 'The claim feature the reserve category belongs to.',
  reserve_category_code VARCHAR NOT NULL COMMENT 'The category of reserve.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Reserve held in this category, in the claim currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the reserve amount.',
  as_at_date DATE NOT NULL COMMENT 'Date the reserve split was struck.',
  source_system_code VARCHAR NOT NULL COMMENT 'Claims system the split originates from.'
)
COMMENT = 'The split of a claim feature''s case reserve by category - indemnity, expense, legal - as at a date. A reporting decomposition of the reserve; the movements themselves remain claim transactions, and the categories sum to the feature''s outstanding reserve. Grain: One row per feature per reserve category per as-at date.';

ALTER TABLE BRICKSURANCE.claim.claim_reserve_category ADD CONSTRAINT pk_claim_reserve_category PRIMARY KEY (claim_reserve_category_id);
