-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.financial_plan (
  financial_plan_id VARCHAR NOT NULL COMMENT 'Identifier for the plan figure.',
  legal_entity_id VARCHAR NOT NULL COMMENT 'The entity the plan is for.',
  plan_version VARCHAR NOT NULL COMMENT 'Plan version, e.g. 2026 Budget v1.',
  line_of_business_code VARCHAR COMMENT 'Line of business, where the plan is at line level.',
  plan_measure VARCHAR NOT NULL COMMENT 'What is planned, e.g. gross_written_premium or combined_ratio.',
  period_label VARCHAR NOT NULL COMMENT 'Period the figure applies to, e.g. 2026-Q3.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Planned value in the stated currency (or a ratio, per plan_measure).',
  currency_code VARCHAR COMMENT 'Currency, for monetary plan measures.'
)
COMMENT = 'A budgeted or forecast figure for a legal entity, line and measure in a period - the plan side of budget-versus-actual. FP&A cannot function without it; pairing it with the actuals metric views is the variance story. Grain: One row per plan version per entity per line per measure per period.';

ALTER TABLE BRICKSURANCE.finance.financial_plan ADD CONSTRAINT pk_financial_plan PRIMARY KEY (financial_plan_id);
