-- Generated by tools/generate.py from bricksurance-data-core model v0.2.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.party.party_role (
  party_role_id VARCHAR NOT NULL COMMENT 'Identifier for this role assignment.',
  party_id VARCHAR NOT NULL COMMENT 'The party playing the role.',
  party_role_type_code VARCHAR NOT NULL COMMENT 'Which role the party plays in the context.',
  policy_id VARCHAR COMMENT 'Policy context of the role, when the role is played on a policy.',
  claim_id VARCHAR COMMENT 'Claim context of the role, when the role is played on a claim.',
  treaty_id VARCHAR COMMENT 'Treaty context of the role, when the role is played on a treaty.',
  start_date DATE NOT NULL COMMENT 'Date from which the role applies (inclusive).',
  end_date DATE COMMENT 'Date on which the role ceased (inclusive); empty while the role is current.'
)
COMMENT = 'A role a party plays in a specific business context - on a policy, a claim or a treaty. This is the model''s central extensibility pattern: a new kind of counterparty is a new party_role_type code plus rows here, never a new table. Grain: One row per role a party plays in one context (policy, claim or treaty) for a period of time. Exactly one of policy_id, claim_id and treaty_id is populated.';

ALTER TABLE BRICKSURANCE.party.party_role ADD CONSTRAINT pk_party_role PRIMARY KEY (party_role_id);
