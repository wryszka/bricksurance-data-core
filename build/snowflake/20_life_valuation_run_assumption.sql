-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.life.valuation_run_assumption (
  valuation_run_id VARCHAR NOT NULL COMMENT 'The valuation run.',
  assumption_set_id VARCHAR NOT NULL COMMENT 'An assumption set the run used.'
)
COMMENT = 'Which assumption sets a valuation run used - one row per assumption set, typically one per assumption type (mortality, lapse, expense, economic). This junction is what makes any past result exactly reproducible: run plus sets plus scenario plus model version is the full recipe. Grain: One row per assumption set used by a valuation run.';

ALTER TABLE BRICKSURANCE.life.valuation_run_assumption ADD CONSTRAINT pk_valuation_run_assumption PRIMARY KEY (valuation_run_id, assumption_set_id);
