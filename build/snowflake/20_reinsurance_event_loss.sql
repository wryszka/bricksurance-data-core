-- Generated by tools/generate.py from bricksurance-data-core model v0.6.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.reinsurance.event_loss (
  event_loss_id VARCHAR NOT NULL COMMENT 'Identifier for the loss row.',
  cat_event_id VARCHAR NOT NULL COMMENT 'The catastrophe event the loss belongs to.',
  treaty_id VARCHAR COMMENT 'Treaty the loss attaches to, for the assumed book.',
  policy_id VARCHAR COMMENT 'Policy the loss attaches to, for the direct book.',
  loss_basis_code VARCHAR NOT NULL COMMENT 'Whether the figure is modelled or reported.',
  gross_loss_amount DECIMAL(18,2) NOT NULL COMMENT 'Gross loss to the contract, in the stated currency.',
  ceded_loss_amount DECIMAL(18,2) COMMENT 'Portion ceded onward under outward protections, where computed.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amounts.',
  as_of_date DATE NOT NULL COMMENT 'Snapshot date of the figure.'
)
COMMENT = 'A loss figure attributed to a catastrophe event - against a treaty (assumed book) or a policy (direct book). The loss basis keeps modelled day-one estimates and firming cedant reports honestly apart; figures are point-in- time as-of snapshots, never overwritten. Grain: One row per event per treaty-or-policy per basis per as-of date.';

ALTER TABLE BRICKSURANCE.reinsurance.event_loss ADD CONSTRAINT pk_event_loss PRIMARY KEY (event_loss_id);
