-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.events.business_event (
  business_event_id VARCHAR NOT NULL COMMENT 'Identifier for the event.',
  business_event_type_code VARCHAR NOT NULL COMMENT 'Kind of event.',
  subject_entity VARCHAR NOT NULL COMMENT 'Entity name of the subject (e.g. quote, claim).',
  subject_id VARCHAR NOT NULL COMMENT 'Identifier of the subject row.',
  occurred_at TIMESTAMP_NTZ NOT NULL COMMENT 'When the event occurred.',
  channel_code VARCHAR COMMENT 'Channel the event arrived through, where relevant.',
  payload VARCHAR COMMENT 'Event payload as JSON, schema per event type.',
  source_system_code VARCHAR NOT NULL COMMENT 'Emitting system.'
)
COMMENT = 'The event stream: business moments (quote requested, loss notified, payment made, complaint received) as append-only, timestamped records referencing the entity they concern. This is the near-real-time face of the model and the natural feed for monitoring agents. Grain: One row per business event occurrence; append-only.';

ALTER TABLE BRICKSURANCE.events.business_event ADD CONSTRAINT pk_business_event PRIMARY KEY (business_event_id);
