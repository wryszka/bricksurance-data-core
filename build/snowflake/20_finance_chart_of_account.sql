-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.chart_of_account (
  account_id VARCHAR NOT NULL COMMENT 'Identifier for the account.',
  account_number VARCHAR NOT NULL COMMENT 'Ledger account number.',
  name VARCHAR NOT NULL COMMENT 'Account name.',
  account_type_code VARCHAR NOT NULL COMMENT 'Asset, liability, equity, income or expense - fixes the normal balance.',
  parent_account_id VARCHAR COMMENT 'Parent account for roll-up; empty at the top of the hierarchy.',
  statement_type_code VARCHAR NOT NULL COMMENT 'Which primary statement the account belongs to.'
)
COMMENT = 'The chart of accounts: every ledger account with its type (which fixes its normal balance) and its parent, forming the account hierarchy that statements roll up. This is what turns classified postings into a real general ledger. Grain: One row per ledger account.';

ALTER TABLE BRICKSURANCE.finance.chart_of_account ADD CONSTRAINT pk_chart_of_account PRIMARY KEY (account_id);
