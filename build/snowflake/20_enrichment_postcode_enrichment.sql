-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.enrichment.postcode_enrichment (
  postcode VARCHAR NOT NULL COMMENT 'Postcode or postal-area key the attributes describe.',
  country_code VARCHAR NOT NULL COMMENT 'Country the postcode belongs to.',
  urbanity_code VARCHAR COMMENT 'Settlement type of the postcode.',
  deprivation_decile NUMBER(38,0) COMMENT 'Index of multiple deprivation decile, 1 (most) to 10 (least).',
  region_code VARCHAR COMMENT 'Statistical region the postcode sits in.',
  source_system_code VARCHAR NOT NULL COMMENT 'Provider the postcode attributes originate from.'
)
COMMENT = 'Area attributes for a postcode - settlement type, deprivation and region - used as rating and accumulation dimensions. Reference-like: keyed by postcode, shared across policies and objects in that area. Grain: One row per postcode.';

ALTER TABLE BRICKSURANCE.enrichment.postcode_enrichment ADD CONSTRAINT pk_postcode_enrichment PRIMARY KEY (postcode);
