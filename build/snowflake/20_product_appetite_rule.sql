-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.appetite_rule (
  appetite_rule_id VARCHAR NOT NULL COMMENT 'Identifier for the rule.',
  product_id VARCHAR COMMENT 'Product the rule scopes to; empty when the rule applies to the whole line.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the rule applies to.',
  rule_order NUMBER(38,0) NOT NULL COMMENT 'Evaluation order; first matching terminal effect wins.',
  description VARCHAR NOT NULL COMMENT 'The rule in business language - what it checks and why it exists.',
  max_sum_insured DECIMAL(18,2) COMMENT 'Upper sum-insured bound for the effect to apply, in the product currency.',
  country_code VARCHAR COMMENT 'Country the rule scopes to, where geographic.',
  appetite_effect_code VARCHAR NOT NULL COMMENT 'What happens when the rule matches.',
  effective_from DATE NOT NULL COMMENT 'First date the rule applies.',
  effective_to DATE COMMENT 'Last date the rule applies; empty while current.'
)
COMMENT = 'An underwriting appetite statement as data: for a product (or a whole line), a bounded condition and its effect - within appetite, refer, or out of appetite. Humans read the same rules agents execute, and every underwriting decision cites the rule it applied. Grain: One row per active appetite rule.';

ALTER TABLE BRICKSURANCE.product.appetite_rule ADD CONSTRAINT pk_appetite_rule PRIMARY KEY (appetite_rule_id);
