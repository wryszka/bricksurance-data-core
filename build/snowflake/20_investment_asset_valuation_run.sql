-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.investment.asset_valuation_run (
  asset_valuation_run_id VARCHAR NOT NULL COMMENT 'Identifier for the run.',
  as_of_date DATE NOT NULL COMMENT 'Valuation date.',
  curve_set VARCHAR COMMENT 'Discount curve set used (e.g. EIOPA RFR 2026-06), reused from solvency2.',
  run_hash VARCHAR COMMENT 'Hash of the run inputs for reproducibility.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the run was executed in.'
)
COMMENT = 'A dated valuation of the asset portfolios against a curve set - the governed run behind position market values and the stress inputs. Named distinctly from the actuarial life.valuation_run to avoid collision. Grain: One row per asset valuation run.';

ALTER TABLE BRICKSURANCE.investment.asset_valuation_run ADD CONSTRAINT pk_asset_valuation_run PRIMARY KEY (asset_valuation_run_id);
