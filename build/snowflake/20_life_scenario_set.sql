-- Generated by tools/generate.py from bricksurance-data-core model v0.4.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.life.scenario_set (
  scenario_set_id VARCHAR NOT NULL COMMENT 'Identifier for the scenario set.',
  name VARCHAR NOT NULL COMMENT 'Human-readable name of the set, including its calibration reference.',
  scenario_status_code VARCHAR NOT NULL COMMENT 'Lifecycle status; exactly one set is ACTIVE for production.',
  scenario_count NUMBER(38,0) COMMENT 'Number of stochastic paths in the set.',
  calibration_date DATE COMMENT 'Market date the set was calibrated to.',
  source_system_code VARCHAR NOT NULL COMMENT 'Provenance of the set (vendor delivery or internal generation).'
)
COMMENT = 'A delivered economic scenario set (risk-free curves, equity paths) used by stochastic valuation. Registered with a lifecycle so exactly one set is active for production runs, and every run records which set it used. Grain: One row per delivered scenario set.';

ALTER TABLE BRICKSURANCE.life.scenario_set ADD CONSTRAINT pk_scenario_set PRIMARY KEY (scenario_set_id);
