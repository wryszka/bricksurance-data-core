-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.reinsurance.cession (
  cession_id VARCHAR NOT NULL COMMENT 'Identifier for the cession.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy being ceded.',
  coverage_id VARCHAR COMMENT 'Coverage being ceded, where cession applies at coverage level.',
  treaty_id VARCHAR NOT NULL COMMENT 'Treaty the business is ceded to.',
  ceded_share DECIMAL(5,4) NOT NULL COMMENT 'Share of the risk ceded under this cession, as a fraction between 0 and 1.'
)
COMMENT = 'The link between direct business and a treaty: which policy (or coverage) is ceded to which treaty, and to what share. Ceded premium and recoveries are derived by applying the ceded share to the underlying transactions. Grain: One row per policy (or coverage, where the split exists) per treaty.';

ALTER TABLE BRICKSURANCE.reinsurance.cession ADD CONSTRAINT pk_cession PRIMARY KEY (cession_id);
