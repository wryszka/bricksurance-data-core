-- Generated by tools/generate.py from bricksurance-data-core model v0.11.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.receivable_transaction (
  receivable_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the movement.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy billed.',
  legal_entity_id VARCHAR COMMENT 'The entity carrying the receivable.',
  receivable_transaction_type_code VARCHAR NOT NULL COMMENT 'Kind of movement; invoices positive, receipts negative.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the stated currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Booking date.',
  due_date DATE COMMENT 'Payment due date, for invoices - the anchor for ageing.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'Premium billing and collection movements on a policy. Written premium is earned exposure; this is the cash reality - outstanding and aged debt are signed sums over these rows. Grain: One row per billing movement per policy.';

ALTER TABLE BRICKSURANCE.finance.receivable_transaction ADD CONSTRAINT pk_receivable_transaction PRIMARY KEY (receivable_transaction_id);
