-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.enrichment.sanctions_screen (
  sanctions_screen_id VARCHAR NOT NULL COMMENT 'Identifier for the screening record.',
  party_id VARCHAR NOT NULL COMMENT 'Party that was screened.',
  screen_date DATE NOT NULL COMMENT 'Date the screening was run.',
  hit_flag BOOLEAN NOT NULL COMMENT 'Whether the screen raised a potential match.',
  pep_flag BOOLEAN COMMENT 'Whether the match was a politically-exposed-person indicator.',
  sanctions_list_code VARCHAR COMMENT 'List the match was raised against, where there was a hit.',
  cleared_flag BOOLEAN COMMENT 'Whether review cleared the hit as a false positive.',
  source_system_code VARCHAR NOT NULL COMMENT 'Screening provider the result originates from.'
)
COMMENT = 'The result of screening a party against sanctions and PEP lists. A hit is an indicator that routes the party to review; it is never an automatic decline. Personal data - classified accordingly. Grain: One row per party per screening run.';

ALTER TABLE BRICKSURANCE.enrichment.sanctions_screen ADD CONSTRAINT pk_sanctions_screen PRIMARY KEY (sanctions_screen_id);
