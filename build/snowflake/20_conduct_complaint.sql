-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.conduct.complaint (
  complaint_id VARCHAR NOT NULL COMMENT 'Identifier for the complaint.',
  complaint_reference VARCHAR NOT NULL COMMENT 'Reference as issued to the complainant.',
  complainant_party_id VARCHAR NOT NULL COMMENT 'The complaining party.',
  policy_id VARCHAR COMMENT 'Policy concerned, where applicable.',
  claim_id VARCHAR COMMENT 'Claim concerned, where applicable.',
  complaint_category_code VARCHAR NOT NULL COMMENT 'Root category.',
  complaint_status_code VARCHAR NOT NULL COMMENT 'Handling status.',
  received_date DATE NOT NULL COMMENT 'Date received.',
  closed_date DATE COMMENT 'Date resolved or referred; empty while open.',
  redress_amount DECIMAL(18,2) COMMENT 'Redress paid, where upheld, in the stated currency.',
  currency_code VARCHAR COMMENT 'Currency of any redress.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record.'
)
COMMENT = 'A customer complaint, categorised per UK FCA reporting groupings and tracked through to ombudsman referral. The conduct domain is where fair treatment becomes measurable. Grain: One row per complaint per source system.';

ALTER TABLE BRICKSURANCE.conduct.complaint ADD CONSTRAINT pk_complaint PRIMARY KEY (complaint_id);
