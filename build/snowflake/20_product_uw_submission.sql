-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.uw_submission (
  uw_submission_id VARCHAR NOT NULL COMMENT 'Identifier for the submission.',
  party_id VARCHAR COMMENT 'Prospect or broker the submission came from.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the submission is for.',
  product_id VARCHAR COMMENT 'Product the submission is against, where the catalog applies.',
  received_date DATE NOT NULL COMMENT 'Date the submission was received.',
  uw_submission_status_code VARCHAR NOT NULL COMMENT 'Status of the submission in the funnel.',
  distribution_channel_code VARCHAR COMMENT 'Channel the submission arrived through.',
  quote_id VARCHAR COMMENT 'Quote produced from the submission, where it proceeded.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the submission originates from.'
)
COMMENT = 'A direct-book submission for cover, upstream of a quote - the request as it arrives from a broker or channel, before it is rated or declined. It gives the underwriting funnel a grain that exists before a quote does; a submission that proceeds produces a quote. Grain: One row per submission per source system.';

ALTER TABLE BRICKSURANCE.product.uw_submission ADD CONSTRAINT pk_uw_submission PRIMARY KEY (uw_submission_id);
