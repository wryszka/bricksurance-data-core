-- Generated by tools/generate.py from bricksurance-data-core model v0.9.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy.endorsement (
  endorsement_id VARCHAR NOT NULL COMMENT 'Identifier for the endorsement.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy being endorsed.',
  endorsement_type_code VARCHAR NOT NULL COMMENT 'Kind of contractual change.',
  effective_date DATE NOT NULL COMMENT 'Date the change takes effect (inclusive).',
  description VARCHAR COMMENT 'Free-text description of the change as recorded.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the endorsement originates from.'
)
COMMENT = 'A mid-term change to a policy contract. The endorsement records the contractual change; any premium effect is booked as premium transactions referencing the same policy, never stored here. Grain: One row per endorsement per policy.';

ALTER TABLE BRICKSURANCE.policy.endorsement ADD CONSTRAINT pk_endorsement PRIMARY KEY (endorsement_id);
