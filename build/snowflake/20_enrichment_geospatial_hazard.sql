-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.enrichment.geospatial_hazard (
  geospatial_hazard_id VARCHAR NOT NULL COMMENT 'Identifier for the hazard record.',
  location_key VARCHAR NOT NULL COMMENT 'Location the hazard describes - postcode, grid reference or gazetteer id. Insured objects carry the same key to join hazard.',
  postcode VARCHAR COMMENT 'Postcode the location falls in, where resolvable.',
  flood_band_code VARCHAR COMMENT 'Modelled flood risk banding.',
  subsidence_band_code VARCHAR COMMENT 'Modelled ground-stability risk banding.',
  crime_index NUMBER(38,0) COMMENT 'Relative crime index for the area, 0-100.',
  coastal_flag BOOLEAN COMMENT 'Whether the location is within a coastal exposure zone.',
  hazard_source_code VARCHAR NOT NULL COMMENT 'Provenance of the hazard classification.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the hazard record was loaded through.'
)
COMMENT = 'Modelled peril hazard for a location - flood, subsidence, crime and coastal exposure - with the source it came from. Used in property rating and catastrophe accumulation. A modelled classification, not a live event state. Grain: One row per location key per hazard source.';

ALTER TABLE BRICKSURANCE.enrichment.geospatial_hazard ADD CONSTRAINT pk_geospatial_hazard PRIMARY KEY (geospatial_hazard_id);
