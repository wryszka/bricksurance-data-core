-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.model.feature_set (
  feature_set_id VARCHAR NOT NULL COMMENT 'Identifier for the feature set.',
  name VARCHAR NOT NULL COMMENT 'Feature set name.',
  description VARCHAR COMMENT 'What the set is for.',
  model_purpose_code VARCHAR COMMENT 'The purpose the set was assembled for.'
)
COMMENT = 'A named collection of features a model version consumes. The set is the contract between features and models; membership is resolved through feature_set_member so the same feature can serve many sets. Grain: One row per feature set.';

ALTER TABLE BRICKSURANCE.model.feature_set ADD CONSTRAINT pk_feature_set PRIMARY KEY (feature_set_id);
