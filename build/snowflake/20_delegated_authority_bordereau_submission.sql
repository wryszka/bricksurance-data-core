-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.delegated_authority.bordereau_submission (
  submission_id VARCHAR NOT NULL COMMENT 'Identifier for the submission.',
  binder_id VARCHAR NOT NULL COMMENT 'Binder the bordereau is submitted under.',
  bordereau_type_code VARCHAR NOT NULL COMMENT 'Kind of bordereau (risk, premium, claims).',
  period_month DATE NOT NULL COMMENT 'Reporting month the bordereau covers.',
  received_date DATE COMMENT 'Date the file was received.',
  due_date DATE NOT NULL COMMENT 'Date the file was due.',
  bordereau_status_code VARCHAR NOT NULL COMMENT 'Processing status of the submission.',
  row_count NUMBER(38,0) COMMENT 'Number of rows in the file.',
  dq_score DECIMAL(6,4) COMMENT 'Data-quality score for the submission, 0 to 1.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the submission was received into.'
)
COMMENT = 'One bordereau file received from a coverholder under a binder for a period - its type, timeliness and data quality. The unit the "which coverholders are late / dirty" view works from. Grain: One row per bordereau file received.';

ALTER TABLE BRICKSURANCE.delegated_authority.bordereau_submission ADD CONSTRAINT pk_bordereau_submission PRIMARY KEY (submission_id);
