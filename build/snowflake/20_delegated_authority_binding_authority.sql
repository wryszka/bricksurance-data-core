-- Generated by tools/generate.py from bricksurance-data-core model v0.15.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.delegated_authority.binding_authority (
  binder_id VARCHAR NOT NULL COMMENT 'Identifier for the binder.',
  coverholder_id VARCHAR NOT NULL COMMENT 'The coverholder the authority is granted to.',
  umr VARCHAR NOT NULL COMMENT 'Unique Market Reference for the binder (format-correct, synthetic).',
  inception_date DATE NOT NULL COMMENT 'Binder inception date.',
  expiry_date DATE NOT NULL COMMENT 'Binder expiry date.',
  territory_scope VARCHAR COMMENT 'Permitted territories as a comma-separated country list; risks outside breach TERRITORY.',
  max_line_size DECIMAL(18,2) COMMENT 'Maximum sum insured the coverholder may bind on one risk.',
  aggregate_limit DECIMAL(18,2) COMMENT 'Maximum cumulative written premium under the binder.',
  premium_estimate DECIMAL(18,2) COMMENT 'Estimated premium income for the binder period.',
  commission_pct DECIMAL(6,4) COMMENT 'Coverholder commission rate on written premium.',
  claims_authority_limit DECIMAL(18,2) COMMENT 'Maximum claim the coverholder may settle without referral.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the binder limits.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the binder is held in.'
)
COMMENT = 'A binder - the contract granting a coverholder authority to bind risks within limits (line size, aggregate, line of business, territory, claims authority). Breaches are measured against these limits. Grain: One row per binding authority (binder).';

ALTER TABLE BRICKSURANCE.delegated_authority.binding_authority ADD CONSTRAINT pk_binding_authority PRIMARY KEY (binder_id);
