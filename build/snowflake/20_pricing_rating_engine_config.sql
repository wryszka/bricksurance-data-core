-- Generated by tools/generate.py from bricksurance-data-core model v0.11.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.pricing.rating_engine_config (
  rating_engine_config_id VARCHAR NOT NULL COMMENT 'Identifier for the rating engine configuration.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the configuration rates.',
  version VARCHAR NOT NULL COMMENT 'Configuration version label.',
  base_rate DECIMAL(18,4) COMMENT 'Base rate the factors build up from, in the config currency.',
  model_version_id VARCHAR COMMENT 'Model version providing the risk premium behind the configuration.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the base rate.',
  effective_from DATE NOT NULL COMMENT 'Date the configuration took effect.',
  effective_to DATE COMMENT 'Date the configuration was superseded; empty while current.'
)
COMMENT = 'A versioned rating configuration for a line of business - the base rate and the model version behind it, effective-dated. This is what a quote''s rating provenance points to, tying a premium to a governed model version. Grain: One row per rating engine configuration version.';

ALTER TABLE BRICKSURANCE.pricing.rating_engine_config ADD CONSTRAINT pk_rating_engine_config PRIMARY KEY (rating_engine_config_id);
