-- Generated by tools/generate.py from bricksurance-data-core model v0.14.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.reinsurance.cat_event (
  cat_event_id VARCHAR NOT NULL COMMENT 'Identifier for the event.',
  event_name VARCHAR NOT NULL COMMENT 'Market name of the event (e.g. a named windstorm).',
  cause_of_loss_code VARCHAR NOT NULL COMMENT 'Primary peril of the event.',
  event_date DATE NOT NULL COMMENT 'Date of the event (first landfall or onset).',
  country_code VARCHAR COMMENT 'Primary country affected, where a single country applies.',
  industry_loss_estimate DECIMAL(18,2) COMMENT 'Market-wide insured loss estimate, in the stated currency.',
  currency_code VARCHAR COMMENT 'Currency of the industry loss estimate.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the event was registered in.'
)
COMMENT = 'A named catastrophe event (windstorm, flood...) that losses accumulate against, across the direct book and the treaty portfolio. Event losses - modelled on day one, reported as cedant figures firm up - reference this event. Grain: One row per catastrophe event.';

ALTER TABLE BRICKSURANCE.reinsurance.cat_event ADD CONSTRAINT pk_cat_event PRIMARY KEY (cat_event_id);
