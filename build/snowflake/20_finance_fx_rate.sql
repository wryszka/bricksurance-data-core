-- Generated by tools/generate.py from bricksurance-data-core model v0.8.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.fx_rate (
  fx_rate_id VARCHAR NOT NULL COMMENT 'Identifier for the rate.',
  from_currency_code VARCHAR NOT NULL COMMENT 'Currency being translated.',
  to_currency_code VARCHAR NOT NULL COMMENT 'Presentation currency translated to.',
  rate_date DATE NOT NULL COMMENT 'Date the rate applies.',
  rate DECIMAL(18,8) NOT NULL COMMENT 'Units of to-currency per one unit of from-currency.'
)
COMMENT = 'A foreign-exchange rate to the group presentation currency at a date - what makes group consolidation across multi-currency subsidiaries a real number instead of a per-currency caveat. Grain: One row per from-currency per rate date.';

ALTER TABLE BRICKSURANCE.finance.fx_rate ADD CONSTRAINT pk_fx_rate PRIMARY KEY (fx_rate_id);
