-- Generated by tools/generate.py from bricksurance-data-core model v0.5.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.policy.premium_transaction (
  premium_transaction_id VARCHAR NOT NULL COMMENT 'Identifier for the premium transaction.',
  policy_id VARCHAR NOT NULL COMMENT 'Policy the premium movement belongs to.',
  coverage_id VARCHAR COMMENT 'Coverage the movement is allocated to, where the source provides that split.',
  premium_transaction_type_code VARCHAR NOT NULL COMMENT 'Kind of premium movement.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount of the movement in the transaction currency: written and positive adjustments positive, returns negative.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  transaction_date DATE NOT NULL COMMENT 'Date the movement was booked.',
  source_system_code VARCHAR NOT NULL COMMENT 'System of record the movement originates from.'
)
COMMENT = 'A signed premium movement on a policy. Premium is transactional by design: figures such as gross written premium are derived by summing transactions, never stored as overwritable balances. Grain: One row per premium movement per policy (and coverage, where known).';

ALTER TABLE BRICKSURANCE.policy.premium_transaction ADD CONSTRAINT pk_premium_transaction PRIMARY KEY (premium_transaction_id);
