-- Generated by tools/generate.py from bricksurance-data-core model v0.10.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.finance.journal_line (
  journal_line_id VARCHAR NOT NULL COMMENT 'Identifier for the line.',
  journal_id VARCHAR NOT NULL COMMENT 'The journal this line belongs to.',
  account_id VARCHAR NOT NULL COMMENT 'The ledger account posted to.',
  amount DECIMAL(18,2) NOT NULL COMMENT 'Signed amount in the entity''s functional currency: debits positive, credits negative. Lines within a journal sum to zero.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the amount.',
  source_entity VARCHAR COMMENT 'Entity name of the originating operational row (e.g. premium_transaction).',
  source_id VARCHAR COMMENT 'Identifier of the originating operational row, for reconciliation.'
)
COMMENT = 'A single debit or credit line of a journal, posted to one ledger account and carrying a signed amount (debits positive, credits negative) plus its source operational transaction. Trial balance and financial statements are sums over these lines; the link to source rows makes the ledger reconcile to the sub-ledgers by construction. Grain: One row per posting line.';

ALTER TABLE BRICKSURANCE.finance.journal_line ADD CONSTRAINT pk_journal_line PRIMARY KEY (journal_line_id);
