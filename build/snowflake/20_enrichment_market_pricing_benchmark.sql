-- Generated by tools/generate.py from bricksurance-data-core model v0.12.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.enrichment.market_pricing_benchmark (
  market_pricing_benchmark_id VARCHAR NOT NULL COMMENT 'Identifier for the benchmark record.',
  line_of_business_code VARCHAR NOT NULL COMMENT 'Line of business the benchmark covers.',
  segment VARCHAR COMMENT 'Market segment or tier the benchmark applies to.',
  benchmark_period VARCHAR NOT NULL COMMENT 'Period the benchmark represents, e.g. 2026-Q2.',
  market_median_premium DECIMAL(18,2) COMMENT 'Median market premium for the segment, in the benchmark currency.',
  rank_percentile DECIMAL(5,2) COMMENT 'Where the insurer''s average price ranks, 0 (cheapest) to 100.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the benchmark premium.',
  source_system_code VARCHAR NOT NULL COMMENT 'Market-intelligence provider the benchmark originates from.'
)
COMMENT = 'External market premium benchmarks by line of business, segment and period - where the insurer''s price sits against the market. Feeds demand modelling and competitive-position views. Commercially sensitive. Grain: One row per line of business per segment per period.';

ALTER TABLE BRICKSURANCE.enrichment.market_pricing_benchmark ADD CONSTRAINT pk_market_pricing_benchmark PRIMARY KEY (market_pricing_benchmark_id);
