-- Generated by tools/generate.py from bricksurance-data-core model v0.16.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.product.referral_rule (
  referral_rule_id VARCHAR NOT NULL COMMENT 'Identifier for this rule version.',
  rule_key VARCHAR NOT NULL COMMENT 'Stable business key of the rule, constant across its versions.',
  name VARCHAR NOT NULL COMMENT 'Human-readable rule name.',
  line_of_business_code VARCHAR COMMENT 'Line of business the rule applies to; empty means all lines.',
  condition_expression VARCHAR NOT NULL COMMENT 'The condition that trips the rule, in business language or a rule expression.',
  referral_action_code VARCHAR NOT NULL COMMENT 'Action taken when the condition is met.',
  version NUMBER(38,0) NOT NULL COMMENT 'Version number of the rule, increasing as it changes.',
  effective_from DATE NOT NULL COMMENT 'Date this version took effect.',
  effective_to DATE COMMENT 'Date this version was superseded; empty while current.',
  is_current BOOLEAN NOT NULL COMMENT 'Whether this is the version currently in force for the rule key.',
  source_system_code VARCHAR NOT NULL COMMENT 'System the rule is managed in.'
)
COMMENT = 'A governed underwriting referral rule - a condition and the action to take when a risk meets it. Rules are effective-dated (a slowly-changing record): a change supersedes the prior version rather than overwriting it, so any past referral can be explained by the rule version in force at the time. Filter is_current for the live rulebook. Grain: One row per rule version; at most one current row per rule key.';

ALTER TABLE BRICKSURANCE.product.referral_rule ADD CONSTRAINT pk_referral_rule PRIMARY KEY (referral_rule_id);
