-- Generated by tools/generate.py from bricksurance-data-core model v0.13.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS BRICKSURANCE.claim.claim_recovery (
  claim_recovery_id VARCHAR NOT NULL COMMENT 'Identifier for the recovery.',
  claim_id VARCHAR NOT NULL COMMENT 'The claim the recovery relates to.',
  recovery_type_code VARCHAR NOT NULL COMMENT 'Source of the recovery.',
  against_party_id VARCHAR COMMENT 'Party the recovery is pursued against, where applicable.',
  expected_amount DECIMAL(18,2) COMMENT 'Recovery anticipated, in the claim currency.',
  received_amount DECIMAL(18,2) COMMENT 'Recovery actually received to date, in the claim currency.',
  currency_code VARCHAR NOT NULL COMMENT 'Currency of the recovery amounts.',
  status VARCHAR COMMENT 'Status of the recovery, e.g. open, received, closed.',
  source_system_code VARCHAR NOT NULL COMMENT 'Claims system the recovery originates from.'
)
COMMENT = 'A recovery pursued or received on a claim, with its source - subrogation, salvage, contribution, reinsurance or deductible. The dimensional detail behind RECOVERY claim transactions: what kind of recovery, against whom, and where it stands. Grain: One row per recovery per claim.';

ALTER TABLE BRICKSURANCE.claim.claim_recovery ADD CONSTRAINT pk_claim_recovery PRIMARY KEY (claim_recovery_id);
