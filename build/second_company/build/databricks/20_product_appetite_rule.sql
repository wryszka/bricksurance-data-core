-- Generated by tools/generate.py from bricksurance-data-core model v0.7.0.
-- Do not edit: change the spec in model/ and regenerate.

CREATE TABLE IF NOT EXISTS lr_serverless_aws_us_catalog.meridian_product.appetite_rule (
  appetite_rule_id STRING NOT NULL COMMENT 'Identifier for the rule.',
  product_id STRING COMMENT 'Product the rule scopes to; empty when the rule applies to the whole line.',
  line_of_business_code STRING NOT NULL COMMENT 'Line of business the rule applies to.',
  rule_order BIGINT NOT NULL COMMENT 'Evaluation order; first matching terminal effect wins.',
  description STRING NOT NULL COMMENT 'The rule in business language - what it checks and why it exists.',
  max_sum_insured DECIMAL(18,2) COMMENT 'Upper sum-insured bound for the effect to apply, in the product currency.',
  country_code STRING COMMENT 'Country the rule scopes to, where geographic.',
  appetite_effect_code STRING NOT NULL COMMENT 'What happens when the rule matches.',
  effective_from DATE NOT NULL COMMENT 'First date the rule applies.',
  effective_to DATE COMMENT 'Last date the rule applies; empty while current.'
)
COMMENT 'An underwriting appetite statement as data: for a product (or a whole line), a bounded condition and its effect - within appetite, refer, or out of appetite. Humans read the same rules agents execute, and every underwriting decision cites the rule it applied. Grain: One row per active appetite rule.';

ALTER TABLE lr_serverless_aws_us_catalog.meridian_product.appetite_rule ADD CONSTRAINT pk_appetite_rule PRIMARY KEY (appetite_rule_id);

ALTER TABLE lr_serverless_aws_us_catalog.meridian_product.appetite_rule SET TAGS ('mm_model' = 'bricksurance-data-core', 'mm_model_version' = '0.7.0', 'mm_domain' = 'product', 'mm_maturity' = 'draft');

ALTER TABLE lr_serverless_aws_us_catalog.meridian_product.appetite_rule ALTER COLUMN appetite_rule_id SET TAGS ('mm_classification' = 'internal');

ALTER TABLE lr_serverless_aws_us_catalog.meridian_product.appetite_rule ALTER COLUMN product_id SET TAGS ('mm_classification' = 'internal');
