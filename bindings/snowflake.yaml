# Snowflake binding — "Bricksurance Specialty" unit in the federated scenario
# (an acquired specialty-lines business on a different stack). Same logical
# model, different physical platform; definitions and keys are generated as
# comments and informational constraints.
platform: snowflake
database: BRICKSURANCE
schema_pattern: "{domain}"
